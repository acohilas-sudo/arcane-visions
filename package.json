{
  "name": "arcane-visions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "npx wrangler pages deploy dist/ --project-name=arcane-visions --branch=main"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
